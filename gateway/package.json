{
  "name": "nous-token-gateway",
  "version": "0.1.0",
  "description": "Transparent LLM API gateway — records real usage from provider responses",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^6.0.2",
    "wrangler": "^4"
  },
  "dependencies": {
    "viem": "^2.47.10"
  }
}
